name: Payment Request Menu
on:
  issues:
    types: [opened]
permissions:
  issues: write
  pull-requests: write
jobs:
  request:
    runs-on: ubuntu-latest
    steps:
      - name: Payment Request Handler
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            try {
              const issue = context.payload.issue;
              const issueNumber = issue.number;
              const issueBody = issue.body || '';
              const issueTitle = issue.title || '';
              
              // Check if this is a payment-related issue
              const paymentKeywords = ['payment', 'checkout', 'plan', 'buy', 'purchase'];
              const isPaymentIssue = paymentKeywords.some(keyword => 
                issueTitle.toLowerCase().includes(keyword) || 
                issueBody.toLowerCase().includes(keyword)
              );
              
              if (!isPaymentIssue) {
                console.log('Not a payment issue, skipping');
                return;
              }
              
              // Check for existing payment menu comment
              const comments = await github.rest.issues.listComments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber
              });
              
              const hasPaymentMenu = comments.data.some(comment => 
                comment.body.includes('💳 Payment Plans Available')
              );
              
              if (hasPaymentMenu) {
                console.log('Payment menu already posted, skipping');
                return;
              }
              
              // Use your actual GitHub Pages URL or deployed domain
              // Replace this with your real domain when deployed
              const checkoutUrl = 'https://sami9889.github.io/Checkoutv4';
              
              // Post payment menu with REAL checkout links
              const menuBody = `## 💳 Payment Plans Available

            Thank you for your interest! Here are our available plans:

            | Plan | Price | Duration | Action |
            |------|-------|----------|--------|
            | **Starter** | $10.00 AUD | 30 days | [Pay Now](${checkoutUrl}/web/checkout.html?plan=starter) |
            | **Basic** | $25.00 AUD | 90 days | [Pay Now](${checkoutUrl}/web/checkout.html?plan=basic) |
            | **Pro** | $99.00 AUD | 1 year | [Pay Now](${checkoutUrl}/web/checkout.html?plan=pro) |

            ### 🏦 Alternative: Bank Transfer

            If you prefer to pay via bank transfer, click here: [Get Payment Code](${checkoutUrl}/web/bank-checkout.html)

            **Bank Details:**
            - **Bank**: Commonwealth Bank (Australia)
            - **Account Name**: SAMRATH SINGH
            - **BSB**: 062948
            - **Account**: 4760652
            - **BIC/SWIFT**: CTBAAU2S

            ### How it works:
            1. **Click the "Pay Now" link** for your desired plan
            2. **Complete payment** via PayPal (instant) or Bank Transfer (manual verification)
            3. **Receive your license key** via email
            4. **Your customer record** will be automatically created
            5. **A new GitHub issue** will be created with your details

            ### What happens after payment:
            ✅ License key sent via email  
            ✅ Customer record created in our system  
            ✅ GitHub issue created with your details  
            ✅ Admin notified of new customer  

            ### Questions?
            Reply to this issue if you have any questions about the payment process.

            ---

            *🔒 All payments are secure and encrypted*`;

              await github.rest.issues.createComment({
                issue_number: issueNumber,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: menuBody
              });
              
              console.log(`✅ Payment menu posted to issue #${issueNumber}`);
              console.log(`   Checkout URL: ${checkoutUrl}`);
              
              // Label the issue
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                labels: ['payment-menu-posted']
              });
              
            } catch (error) {
              console.error('❌ Error details:', error.message);
              console.error('   Full error:', JSON.stringify(error, null, 2));
              throw error;
            }
